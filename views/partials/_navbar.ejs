<nav>
  <!-- add background color, border radius, hover actions, etc... -->

  <% if(user) {%>
  <!-- <a href="/users/< %= user._id %>/workouts/new">Add Workout</a> ???-->
  <div class="nav-container">

    <a class="view-workouts-link" href="/users/<%= user._id %>/workouts">
      <div class="-view-workouts-text">View Workouts</div>
    </a>

    <a class="add-workout-link" href="/users/<%= user._id %>/workouts/new">
      <div class="add-workout-text">Add Workout</div>
    </a>

    <a class="our-community-link" href="/users/<%= user._id %>/workouts/new">
      <div class="our-community-text">Our Community</div>
    </a>
    
    <a class="sign-out-link" href="/auth/sign-out">
      <div class="sign-out-text">Sign Out</div>
    </a>
  </div>

  <% } else { %>
  <!-- add conditional for community page -->
  <% } %>
</nav>
